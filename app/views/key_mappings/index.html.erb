<h1>Listing key_mappings</h1>

<table>
  <tr>
    <th>Key</th>
    <th>Content</th>
    <th>Ordinal</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @key_mappings.each do |key_mapping| %>
  <tr>
    <td><%= key_mapping.key %></td>
    <td><%= key_mapping.content %></td>
    <td><%= key_mapping.ordinal %></td>
    <td><%= link_to 'Show', group_key_mapping_path(@group_id, key_mapping) %></td>
    <td><%= link_to 'Edit', edit_group_key_mapping_path(@group_id, key_mapping) %></td>
    <td><%= link_to 'Destroy', group_key_mapping_path(@group_id, key_mapping), method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Key mapping', new_group_key_mapping_path %>
